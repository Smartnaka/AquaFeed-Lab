"use client";
import { useEffect, useState } from "react"; import { defaultIngredients } from "@/lib/ingredients"; import { getIngredients, saveIngredients } from "@/lib/storage"; import type { Ingredient } from "@/lib/types"; import { Button } from "@/components/ui/button"; import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"; import { Input } from "@/components/ui/input"; import { Table,TBody,TD,TH,THead,TR } from "@/components/ui/table";
export default function Ingredients(){ const [items,setItems]=useState<Ingredient[]>([]); useEffect(()=>setItems(getIngredients()),[]); function update(id:string,v:number){ const next=items.map(i=>i.id===id?{...i,proteinPercentage:v}:i); setItems(next); saveIngredients(next);} function reset(){setItems(defaultIngredients); saveIngredients(defaultIngredients)} return <Card><CardHeader><CardTitle>Ingredient Database</CardTitle><CardDescription>Default protein values are editable and saved locally for future calculations.</CardDescription></CardHeader><CardContent className="space-y-4">{items.length===0?<p>Loading ingredients...</p>:<Table><THead><TR><TH>Ingredient name</TH><TH>Category</TH><TH>Protein percentage</TH></TR></THead><TBody>{items.map(i=><TR key={i.id}><TD>{i.name}</TD><TD>{i.category}</TD><TD><Input className="max-w-28" type="number" value={i.proteinPercentage} onChange={e=>update(i.id,Number(e.target.value))}/></TD></TR>)}</TBody></Table>}<Button variant="outline" onClick={reset}>Reset defaults</Button></CardContent></Card>}
