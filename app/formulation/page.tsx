"use client";
import { useState } from "react"; import { calculateFormulation } from "@/lib/calculations"; import { getIngredients, saveCalculation } from "@/lib/storage"; import type { FormulationResult } from "@/lib/types"; import { Button } from "@/components/ui/button"; import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"; import { Input } from "@/components/ui/input"; import { Table,TBody,TD,TH,THead,TR } from "@/components/ui/table"; import { ResultActions } from "@/components/ResultActions";
export default function Formulation(){ const [kg,setKg]=useState("10"),[target,setTarget]=useState("25"),[result,setResult]=useState<FormulationResult|null>(null),[error,setError]=useState(""); function submit(){ setError(""); try{ const total=Number(kg), t=Number(target); if(!total||!t||total<=0||t<=0) throw new Error("Enter positive values for feed quantity and target protein."); const r=calculateFormulation(total,t,getIngredients()); setResult(r); saveCalculation(r);}catch(e){setError(e instanceof Error?e.message:"Unable to calculate formulation.")}} return <div className="space-y-6"><Card><CardHeader><CardTitle>Fish Feed Formulation Calculator</CardTitle><CardDescription>Uses average protein and energy groups, Pearson square normalization, and 2% fixed additives.</CardDescription></CardHeader><CardContent className="grid gap-4 md:grid-cols-3"><label className="text-sm font-medium">Total feed quantity (kg)<Input type="number" value={kg} onChange={e=>setKg(e.target.value)} /></label><label className="text-sm font-medium">Target crude protein (%)<Input type="number" value={target} onChange={e=>setTarget(e.target.value)} /></label><div className="flex items-end"><Button onClick={submit}>Formulate feed</Button></div>{error&&<p className="md:col-span-3 rounded-md bg-red-50 p-3 text-sm text-red-700">{error}</p>}</CardContent></Card>{result?<Card><CardHeader><CardTitle>Batch result: {result.totalKg.toFixed(2)} kg</CardTitle><CardDescription>{result.explanation}</CardDescription></CardHeader><CardContent className="space-y-4"><div className="grid gap-3 md:grid-cols-4"><div className="rounded-lg bg-secondary p-3">Protein avg: <b>{result.highProteinAverage.toFixed(1)}%</b></div><div className="rounded-lg bg-secondary p-3">Energy avg: <b>{result.lowProteinAverage.toFixed(1)}%</b></div><div className="rounded-lg bg-secondary p-3">Protein mix: <b>{result.proteinMixPercentage.toFixed(1)}%</b></div><div className="rounded-lg bg-secondary p-3">Fixed: <b>{result.fixedPercentage}%</b></div></div><Table><THead><TR><TH>Ingredient</TH><TH>Category</TH><TH>Protein %</TH><TH>Weight kg</TH></TR></THead><TBody>{result.ingredients.map(i=><TR key={i.id}><TD>{i.name}</TD><TD>{i.category}</TD><TD>{i.proteinPercentage}%</TD><TD>{i.weightKg.toFixed(2)}</TD></TR>)}</TBody></Table><ResultActions title="Feed Formulation Result" lines={[`Target protein: ${result.targetProteinPercentage}%`, `Total feed: ${result.totalKg.toFixed(2)} kg`, ...result.ingredients.map(i=>`${i.name}: ${i.weightKg.toFixed(2)} kg`)]}/></CardContent></Card>:<Card><CardHeader><CardTitle>Empty result</CardTitle><CardDescription>Run a formulation to see ingredient quantities.</CardDescription></CardHeader></Card>}</div>}
